# Copy to .env and fill in the secrets before running `npm run start:dev`

# Used to sign session + handshake tokens (replace with a strong random string)
SESSION_SECRET=change_me

# Provide Firebase Admin credentials via ONE of the following:

# Option A: raw JSON string (single line)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"metavrapps",...}

# Option B: base64‑encoded JSON (set this instead of the raw JSON)
# FIREBASE_SERVICE_ACCOUNT_KEY=ewoJInR5cGUiOiAic2VydmljZV9hY2NvdW50Iiwg...

# Allowed dashboard origins for CORS (comma-separated)
DASHBOARD_ORIGIN=http://localhost:3000

# Optional Nest port (defaults to 4000)
# PORT=4000